name: Build and Deploy Docker

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Cloud SDK
      uses: google-github-actions/setup-gcloud@v1
      with:
        service_account_key: ${{ secrets.GCP_CREDENTIALS2 }}
        project_id: notelyy

    - name: Build and push Docker image
      run: |
        gcloud builds submit --tag us-central1-docker.pkg.dev/notelyy/notely-test/notelyy:latest .
